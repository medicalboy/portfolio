{
  "name": "wilson-li-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio site for Wilson Li — React client, Node/Express content API.",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix client install",
    "gen:profile": "node scripts/gen-profile.mjs",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm run gen:profile && npm --prefix client run dev",
    "build": "npm run gen:profile && npm --prefix client run build",
    "start": "npm run build && npm --prefix server start",
    "deploy": "./scripts/deploy.sh"
  }
}
